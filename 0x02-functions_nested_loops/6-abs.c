#include "main.h"

/**
 * _abs - Calculates the absolute value
 *
 * Return: The absolute value
 *
 * @n: The number
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = -1 * n;
		return (n);
	}
	else
	{
		return (n);
	}
}
