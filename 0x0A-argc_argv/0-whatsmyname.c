#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 * Return: zero
 * @argc: Number of arguments
 * @argv: The array of arguments
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
